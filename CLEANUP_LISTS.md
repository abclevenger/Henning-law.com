# Henning Law Repository Cleanup – KEEP / REMOVE Lists

## 1) Henning Law app identification

- **Framework:** Next.js **App Router** (`src/app/`).
- **Homepage:** `src/app/page.tsx` (uses `Hero`, translations, metadata).
- **Site title (metadata):** `Henning Law Firm PLLC | U.S. Immigration & Visa for German and International Clients` in `src/data/metadataByLang.ts`.
- **Domain:** henning-law.com / henning-law-com.vercel.app.

This repository contains **one** Next.js application (Henning Law). There are no other app roots or separate sites; only template/planning artifacts are removed.

---

## 2) Dependency graph (summary)

**Routes/pages (Henning Law):**  
`/`, `/accessibility`, `/attorney-bio`, `/blog`, `/contact`, `/corporate-business-support`, `/employment-investment-residency`, `/family-immigration-marriage`, `/faqs`, `/practice-areas`, `/practice-areas/[slug]`, `/privacy`, `/real-estate-estate-planning`, `/terms`, `/testimonials`, `/travel-work-visas`, `/us-citizenship`.

**Components used:**  
Navbar, Footer, Hero, ScrollReveal, LayoutA11yLinks, LanguageToggle, Icons, BookingWidget, ContactCTA, PracticeAreaPageHero.

**Data/context:**  
`@/data/translations`, `@/data/images`, `@/data/practiceAreas`, `@/data/metadataByLang`, `@/constants/languages`, `@/contexts/LanguageContext`.

**Config required to build:**  
`package.json`, `package-lock.json`, `next.config.ts`, `tsconfig.json`, `eslint.config.mjs`, `.gitignore`.

---

## 3) KEEP (required for Henning Law)

| Item | Reason |
|------|--------|
| `package.json` | Build and scripts. |
| `package-lock.json` | Dependency lockfile. |
| `next.config.ts` | Next.js build config. |
| `tsconfig.json` | TypeScript config. |
| `eslint.config.mjs` | Lint config (used by `next lint`). |
| `.gitignore` | Repo ignore rules. |
| `src/` (entire tree) | App router, components, data, contexts, constants. |
| `public/favicon.png` | Referenced in root layout. |
| `public/images/*` (all image files) | Referenced by Hero, layout, images.ts, and pages. |

---

## 4) REMOVE (with reasons)

| Item | Reason |
|------|--------|
| `.next/` | Build cache; regenerated by `next build` / `next dev`. |
| `NEW_SCOPE.json` | Planning/scope documentation; not required for build or run. |
| `OLD_SCOPE.json` | Planning/scope documentation; not required for build or run. |
| `REPOSITIONING_SUMMARY.md` | Planning documentation; not required for build or run. |
| `README.md` | Default create-next-app template README; not Henning Law–specific. |
| `public/next.svg` | Default Next.js template asset; not referenced in code. |
| `public/vercel.svg` | Vercel template asset; not referenced in code. |
| `public/file.svg` | Template asset; not referenced in code. |
| `public/window.svg` | Template asset; not referenced in code. |
| `public/globe.svg` | Template asset; not referenced in code. |
| `public/images/README.md` | In-asset documentation; not required for build. |

---

*Generated during cleanup. Can be deleted after verification.*
